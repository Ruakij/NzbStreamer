version: "2"

linters:
  exclusions:
    presets:
      # Close, Flush and os.Remove on a cleanup path. Every one of them here is a
      # read-side close with nothing to report the error to; this is a read-only
      # filesystem, so no unwritten bytes are hiding behind one.
      - std-error-handling
